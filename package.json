{
  "name": "react-portfolio",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "heroku-postbuild": "cd react-portfolio && npm install --only=dev && npm install && npm run build",
    "server": "node server/index.js",
    "start": "node server/index.js",
    "dev": "concurrently \"nodemon server/index.js\" \"cd react-portfolio && npm run start\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.16.4"
  },
  "devDependencies": {
    "concurrently": "^5.0.1",
    "nodemon": "^1.19.4",
    "ws": "3.3.2"
  }
}
